{
  "compilerOptions": {
    "outDir": "./dist",                // Diretório de saída
    "rootDir": "./src",                // Diretório de entrada para arquivos TS
    "module": "CommonJS",              // Para gerar módulos CommonJS
    "moduleResolution": "node",        // Usar resolução de módulos do Node.js (padrão para CommonJS)
    "target": "ES2020",                // Definir a versão alvo do JavaScript
    "esModuleInterop": true,           // Habilitar interoperação entre módulos ESM e CommonJS
    "strict": true,                    // Ativar verificação estrita de tipos
    "types": ["@types/aws-lambda"]    // Tipos para AWS Lambda
  },
  "include": ["src/**/*.ts"],          // Incluir todos os arquivos TypeScript dentro da pasta src
  "exclude": ["node_modules"]          // Excluir node_modules
}